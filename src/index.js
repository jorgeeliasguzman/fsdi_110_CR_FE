import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';
import reportWebVitals from './reportWebVitals';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

// If you want to start measuring performance in your app, pass a function
// to log results (for example: reportWebVitals(console.log))
// or send to an analytics endpoint. Learn more: https://bit.ly/CRA-vitals
reportWebVitals();


/**
*
* In cart cmp
* get the cart (array) from the context 
* map the aeeay into strinf (x.title)
* chula. fyi. im getting way better at touch typing with a few typos here and there. But much better than before. if yopu ask me, i'll be a pro in no time
*
*/